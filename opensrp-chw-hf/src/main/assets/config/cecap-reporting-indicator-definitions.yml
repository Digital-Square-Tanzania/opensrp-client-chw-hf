indicators:
  - key: "cecap-1-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
  - key: "cecap-1-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
  - key: "cecap-1-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
  - key: "cecap-1-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

    
  - key: "cecap-1-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

    
  - key: "cecap-1-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

    
  - key: "cecap-1-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

    
  - key: "cecap-1-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
    
#    cecap-1-hiv-negative
  
  - key: "cecap-1-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  - key: "cecap-1-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  - key: "cecap-1-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  - key: "cecap-1-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-1-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-1-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-1-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-1-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
    
#    cecap-1-hiv-unknown
  
  - key: "cecap-1-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  - key: "cecap-1-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  - key: "cecap-1-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  - key: "cecap-1-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-1-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-1-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-1-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-1-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND via_findings is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
  - key: "cecap-2-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-2-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-2-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-2-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-2-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-2-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-2-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-2-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
#    CECAP 2 HIV Negative
  
  
  - key: "cecap-2-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
#    CECAP 2 HIV Unknown
  
  
  - key: "cecap-2-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
  
  
  - key: "cecap-2-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND hpv_dna_specimen_sample_id is NOT NULL
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 34
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
 
  - key: "cecap-3-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 34
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
 
  - key: "cecap-3-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 34
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-3-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with HPV DNA (outreach and facility)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (hpv_dna_specimen_sample_id is NOT NULL OR via_findings is NOT NULL)
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-4-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women with positive VIA results "
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-5-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-6-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women with positive HPV-DNA results identified VIA-positive"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ectr.hpv_dna_findings = 'positive'
                     AND via_findings LIKE '%positive%'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-7-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-7-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-7-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-7-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-7-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-7-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-7-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-7-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-8-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women screened for the first time with VIA positive large lesion"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'suspicious'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-9-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using thermo the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'thermocoagulation'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-10-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with small or moderate  lesion treated using cryotherapy the same day"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_small_lesion'
                     AND treatment_option = 'cryotherapy'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 34
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 34
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 34
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
  - key: "cecap-11-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women screened VIA positive with large lesion treated using LEEP"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings = 'positive_with_large_lesion'
                     AND leep_performed = 'yes'
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'negative'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-12-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women with VIA Positive received treatment using LEEP, Cryo or Thermo (postponed+treated on same day)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND via_findings LIKE '%positive%'
                     AND (leep_performed = 'yes' OR treatment_option = 'cryotherapy' OR treatment_option = 'thermocoagulation')
                     AND ecr.hiv_status = 'unknown'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-14-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women tested HIV positive and linked to CTC during CECAP services"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-14-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women tested HIV positive and linked to CTC during CECAP services"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-14-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women tested HIV positive and linked to CTC during CECAP services"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-14-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women tested HIV positive and linked to CTC during CECAP services"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-14-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women tested HIV positive and linked to CTC during CECAP services"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-14-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women tested HIV positive and linked to CTC during CECAP services"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-14-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women tested HIV positive and linked to CTC during CECAP services"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-14-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women tested HIV positive and linked to CTC during CECAP services"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

 
 
  - key: "cecap-15-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination IS NOT NULL
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-positive-50+"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'positive'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-negative-50+"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'negative'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-16-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Number of women received breast cancer examination (CBE) with abnormal results (subset of indicator 15)"
    indicatorQuery: "SELECT count(DISTINCT ecr.base_entity_id) as count
                     FROM ec_cecap_register ecr
                     INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                     INNER JOIN ec_cecap_visit ecv ON ecv.entity_id = ecr.base_entity_id
                     WHERE 50 <= (date('Now') - date(efm.dob))
                     AND (efm.gender='Female')
                     AND ecr.hiv_status = 'unknown'
                     AND clinical_breast_examination NOT LIKE '%none%'
                     AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                     date(substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                     '-' || substr(strftime('%Y-%m-%d', datetime(ecv.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
#    CECAP 17
  - key: "cecap-17-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-positive-50+"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 50 <= (date('Now') - date(efm.dob))
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

  - key: "cecap-17-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-negative-50+"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 50 <= (date('Now') - date(efm.dob))
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

  - key: "cecap-17-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-17-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya mwaka mmoja (waliowahi kutibiwa kwa thermo, cryo au LEEP)"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 50 <= (date('Now') - date(efm.dob))
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND (ecv1.treatment_option LIKE '%cryotherapy%' OR ecv1.treatment_option LIKE '%thermocoagulation%'  OR ecv1.leep_performed = 'yes')
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 31536000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
#    CECAP 18
  - key: "cecap-18-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-positive-50+"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 50 <= (date('Now') - date(efm.dob))
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

  - key: "cecap-18-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-negative-50+"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 50 <= (date('Now') - date(efm.dob))
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

  - key: "cecap-18-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-18-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitatu ya  uchunguzi-VIA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 50 <= (date('Now') - date(efm.dob))
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ecv1.via_findings LIKE '%negative%'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
    
#    CECAP 19
  - key: "cecap-19-hiv-positive-15-19"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-positive-20-24"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-positive-25-29"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-positive-30-34"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-positive-35-39"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-positive-40-44"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-positive-45-49"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-positive-50+"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 50 <= (date('Now') - date(efm.dob))
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'positive'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

  - key: "cecap-19-hiv-negative-15-19"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-negative-20-24"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-negative-25-29"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-negative-30-34"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-negative-35-39"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-negative-40-44"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-negative-45-49"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-negative-50+"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 50 <= (date('Now') - date(efm.dob))
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'negative'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"

  - key: "cecap-19-hiv-unknown-15-19"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 15 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 20
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-unknown-20-24"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 20 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 25
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-unknown-25-29"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 25 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 30
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-unknown-30-34"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 30 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 35
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-unknown-35-39"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 35 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 40
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-unknown-40-44"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 40 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 45
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-unknown-45-49"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 45 <= (date('Now') - date(efm.dob))  AND (date('Now')-date(efm.dob)) < 50
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"
 
 
  - key: "cecap-19-hiv-unknown-50+"
    grouping: "CECAP"
    description: "Idadi ya wanawake wa hudhurio la marudio baada ya miaka mitano ya uchunguzi - HPV DNA negative"
    indicatorQuery: " SELECT COUNT(DISTINCT ecr.base_entity_id) AS count
                      FROM ec_cecap_register ecr
                      INNER JOIN ec_family_member efm ON efm.base_entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_visit ecv1 ON ecv1.entity_id = ecr.base_entity_id
                      INNER JOIN ec_cecap_test_results ectr ON ectr.entity_id = ecv1.base_entity_id
                      INNER JOIN ec_cecap_visit ecv2 ON ecv2.entity_id = ecr.base_entity_id AND ecv1.last_interacted_with < ecv2.last_interacted_with
                      WHERE 50 <= (date('Now') - date(efm.dob))
                      AND efm.gender = 'Female'
                      AND ecr.hiv_status = 'unknown'
                      AND ectr.hpv_dna_findings = 'negative'
                      AND ABS(ecv2.last_interacted_with/1000 - ecv1.last_interacted_with/1000) >= 94608000
                      AND date((substr('%s', 1, 4) || '-' || substr('%s', 6, 2) || '-' || '01')) =
                      date(substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 1, 4) ||
                      '-' || substr(strftime('%Y-%m-%d', datetime(ecv2.last_interacted_with / 1000, 'unixepoch', 'localtime')), 6, 2) || '-' || '01')"



